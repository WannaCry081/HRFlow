const DashboardPage = () => {
    return (
        <section className="h-screen w-screen flex items-center justify-center">
            <h1>Dashboard</h1>
        </section>
    );
};

export default DashboardPage;